{
	"name": "lomerge",
	"version": "1.0.1",
	"description": "CLI tool to merge JSON files.",
	"main": "lomerge.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "rektide de la faye",
	"license": "X",
	"dependencies": {
		"lodash.isarray": "^4.0.0",
		"lodash.mergewith": "^4.6.0"
	}
}
